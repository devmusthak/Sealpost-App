/// Splash animation and navigation timing.
abstract final class SplashConstants {
  static const Duration animationDuration = Duration(milliseconds: 2200);
  static const Duration navigateDelay = Duration(milliseconds: 3000);
  static const Duration routeTransition = Duration(milliseconds: 450);
}
